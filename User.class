//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by FernFlower decompiler)
//

import java.util.ArrayList;

public class User {
    private static int idCounter = 1;
    private int id;
    private String name;
    private int age;
    private double balance;
    private ArrayList<Ticket> orderHistory;

    public User(String name, int age, double balance) {
        this.id = idCounter++;
        this.name = name;
        this.age = age;
        this.balance = balance;
        this.orderHistory = new ArrayList();
    }

    public int getId() {
        return this.id;
    }

    public String getName() {
        return this.name;
    }

    public int getAge() {
        return this.age;
    }

    public double getBalance() {
        return this.balance;
    }

    public ArrayList<Ticket> getOrderHistory() {
        return this.orderHistory;
    }

    public void addTicket(Ticket ticket) {
        this.orderHistory.add(ticket);
    }

    public boolean deductBalance(double amount) {
        if (this.balance >= amount) {
            this.balance -= amount;
            return true;
        } else {
            System.out.println("Not enough balance.");
            return false;
        }
    }

    public void showOrderHistory() {
        if (this.orderHistory.isEmpty()) {
            System.out.println("No tickets purchased.");
        } else {
            for(Ticket ticket : this.orderHistory) {
                System.out.println(ticket);
            }
        }

    }

    public String toString() {
        return "User ID: " + this.id + ", Name: " + this.name + ", Age: " + this.age + ", Balance: $" + this.balance;
    }
}
